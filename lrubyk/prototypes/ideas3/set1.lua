-- when we save a setting into a library: how do we create inlets/outlets ?
rk.set('a', {class='proto.lua', name='A'})
-- links
--a.output:link('b','input')
rk.set('b', {class='proto.lua', name='B'})

